<%= form_with(model: person) do |form| %>
  <% if person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(person.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
        <% person.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :home_phone %>
    <%= form.text_field :home_phone, data: { "phone-input" => "home-phone", "phone-input-normalized" => "squash" } %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<script>
import("/javascripts/phone_input_international.js").then(
    function (pii) {
        pii.attach("phone-input");

        pii.onChange(function (event, id, element, valid, normalized) {
            console.log("PII Event starting");
            console.log(event);
            console.log(id);
            console.log(element);
            console.log(valid);
            console.log(normalized);
            console.log("PII Event complete");
        });
    }
);
</script>
<%# 

<h2>Input</h2>

<form id="phoneform">
<label>Land Line</label>
<input type="tel" data-telephone-input="landLine"></input>
<br>
<label>Work Number</label>
<input type="tel" data-telephone-input="workNumber"></input>
<br>
<label>SMS Number</label>
<input type="tel" data-phone-input="smsNumber"></input>
<br>
<label>SMS Number (normalized)</label>
<input type="tel" data-phone-input="smsNumber" data-phone-input-normalized="squash"></input>
</form>

<h2>Callbacks</h2>

Your land line is <span id="landLine"></span>.
<br>
Your work number is <span id="workNumber"></span>.
<br>
Your SMS number is <span id="smsNumber"></span>.

<h2> Form Data</h2>

<pre id="formdata"></pre> %>

